# CloudOpsAgent local configuration template.
# Copy to .env for local use. Do NOT commit .env and do NOT put real secrets here.

# Simulated application service
APP_SERVICE_PORT=8001
APP_LOG_LEVEL=INFO

# Simulation controls (bounded, local-only). Set APP_SIMULATION_ENABLED=false to
# disable the /simulate/* endpoints outside local development.
APP_SIMULATION_ENABLED=true
APP_SIMULATION_MAX_DURATION_SECONDS=300
APP_SIMULATION_DEFAULT_DURATION_SECONDS=30

# Consultable in-memory log store.
APP_MAX_LOG_ENTRIES=500

# Simulated scaling (deterministic; not a fault, survives /simulate/reset).
APP_BASELINE_REPLICAS=2
APP_MIN_REPLICAS=1
APP_MAX_REPLICAS=10

# Agent service
AGENT_SERVICE_PORT=8000
AGENT_LOG_LEVEL=INFO
AGENT_APP_BASE_URL=http://app:8001
AGENT_POLL_INTERVAL_SECONDS=10

# Monitoring module (internal to the agent).
AGENT_MONITORING_ENABLED=true
AGENT_TRAFFIC_SPIKE_RPS_THRESHOLD=20.0
AGENT_APP_REQUEST_TIMEOUT_SECONDS=5.0

# Post-remediation verification thresholds (strictly-below comparisons).
# traffic_spike at 2 replicas is CPU 78 / latency 180 (fails); at 4 replicas it
# is CPU 39 / latency 90 (passes), so the defaults separate them.
AGENT_HEALTHY_CPU_THRESHOLD=70.0
AGENT_HEALTHY_LATENCY_MS_THRESHOLD=150.0

# LLM provider: AWS Bedrock (Claude Sonnet 4.5 via cross-region inference).
# The client lives in cloudops_agent.graph.model.connection so it can be swapped.
AGENT_LLM_PROVIDER=aws-bedrock
AGENT_BEDROCK_MODEL_ID=us.anthropic.claude-sonnet-4-5-20250929-v1:0
AWS_REGION=us-east-1
# Bedrock API key (bearer token). Leave empty to use the standard AWS credential
# chain (profile, instance role, etc.). Never commit a real key.
AWS_BEARER_TOKEN_BEDROCK=