"""Model connection boundary for the agent.

This package is the single place where the workflow talks to a model provider.
Phase 5 will build the deterministic graph on top of
``cloudops_agent.graph.model.connection.get_model``.
"""
